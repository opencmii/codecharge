# Set default behaviour, in case users don't have core.autocrlf set.
* text=auto

# These files are text and should be normalized (convert crlf => lf)
# Les fichiers suivants sont du texte et doivent être normalisés (crlf => lf)  

# Extension standards
*.js      text
*.css     text
*.html    text
*.php     text
*.xml     text
*.txt     text

# Extensions spécifiques à CodechargeStudio
*.ccs 	text
*.ccp	text


# Images should be treated as binary.
# Images doivent être traitées comme des fichiers binaires.
# (binary is a macro for -text -diff)
*.png     binary
*.jpeg    binary
*.gif     binary
*.avi	  binary